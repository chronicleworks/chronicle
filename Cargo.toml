[workspace]

members = [
    "common",
    "api",
    "cli",
    "sawtooth_protocol",
    "frontend"
]


[profile.release]
lto = true
opt-level = 'z'
codegen-units = 1

[profile.release.build-override]
opt-level = 'z'
codegen-units = 1
