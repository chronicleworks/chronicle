[workspace]

members = [
  "common",
  "api",
  "chronicle",
  "chronicle-domain-test",
  "chronicle-untyped",
  "sawtooth-protocol",
  "sawtooth-tp",
  "telemetry",
]


[profile.release]
codegen-units = 1
lto           = true
opt-level     = 'z'

[profile.release.build-override]
codegen-units = 1
opt-level     = 'z'
